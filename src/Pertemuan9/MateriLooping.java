package Pertemuan9;

import java.util.Scanner;

public class MateriLooping {
    public static void main(String[] args) {
        // Scanner sc = new Scanner(System.in);
        // int i = sc.nextInt();
        // sc.close();
        //
        // while (i > 0) {
        // System.out.print(i + " ");
        // i -= 2;
        // }
        //
        // i = 0;
        // for (int y = 10; i < 5 && y <= 12; i++, y++) {
        // System.out.printf("%d %d\n", i, y);
        // }
        //
        // boolean terus = true;
        // for (; terus;) {
        // System.out.println("lanjut terus");
        // }

        System.out.println('1' - '0');

        // for (int i = 0; i < d.length(); i += 2) {
        // x = Integer.parseInt(d.substring(i, i + 1));
        // ganjil += x;
        // }
        // for (int i = 1; i < d.length(); i += 2) {
        // x = Integer.parseInt(d.substring(i, i + 1));
        // genap += x;
        // }
        //
        // for (int i = 0; i < d.length(); i += 2) {
        // x = Integer.parseInt(d.substring(i, i + 1));
        // ganjil += x;
        // x = Integer.parseInt(d.substring(i + 1, i + 2));
        // genap += x;
        // }
    }
}
